# Django Settings
SECRET_KEY=django-insecure-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# For development, leave USE_POSTGRES=False to use SQLite
# For production, set to True and configure PostgreSQL
USE_POSTGRES=False
DB_NAME=connecmaq_db
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_HOST=localhost
DB_PORT=5432

# CORS Settings (for frontend)
# Add your frontend URLs separated by commas
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Email Configuration (for production)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your_email@gmail.com
# EMAIL_HOST_PASSWORD=your_app_password
# DEFAULT_FROM_EMAIL=noreply@connecmaq.com

# Redis Configuration (for production WebSockets with Channels)
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Stripe/Payment Configuration (future)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLISHABLE_KEY=pk_test_...

